// [레이아웃] 비로그인 전용 (signin / signup)

/*
@ TODO: 인증 가드
- AuthProvider 작업 시 로그인 사용자는 진입 불가 처리
*/

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
